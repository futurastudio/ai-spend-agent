<!-- GENERATED by scripts/generate-local-agent-source-docs.mjs. Do not edit by hand. -->

# Claude Code local transcripts

[All local agent sources](./README.md)

| Property | Registry value |
| --- | --- |
| Format ID | `claude-code` |
| Provider | `anthropic` |
| Format | `JSON Lines under ~/.claude/projects/**/*.jsonl` |
| Default local root | `~/.claude/projects` |
| Discovered files | `*.jsonl` |
| Presence-only signal | `none` |
| Financial reader | `full_jsonl` |
| Reader validation | `live_verified` |
| Priced financial evidence | `estimated` |
| Unpriced financial evidence | `missing` |
| Normalized source | `Local agent session logs` |
| Observed from | `claude-code transcript JSONL (this machine)` |
| Usage granularity | `daily_aggregate` |

Local transcript parsing is exercised against live logs; dollar values remain API-rate estimates.

Reader validation describes how the parser has been exercised; it never upgrades the evidence quality of a financial number. Source freshness and the latest sanitized reader error remain runtime observations shown by `aibill doctor --sources`, not static claims from this page.

## How aibill reads it

- Read one JSON object per line and keep assistant records with message.usage.
- Ignore &lt;synthetic&gt; placeholder models and deduplicate streaming/retry rewrites by message and request identity.
- Treat each retained assistant usage record as turn-scoped evidence.

## Fields read

- timestamp, model, and token-usage components
- session and working-directory metadata for local deduplication and attribution
- human-prompt and tool metadata for privacy-reduced local activity summaries

## Evidence boundary

### Verified about the reader

- The reader and its failure paths have been exercised against live local Claude Code logs.
- Synthetic recorded fixtures lock the supported JSONL shapes and normalized output.

### Estimated financial evidence

- Supported token components are priced at published API rates as API-equivalent value.

### Not verified

- API-equivalent value is not billed spend, subscription cost, savings, or ROI.
- Claude Code transcripts do not provide account-plan headroom; missing limits are not inferred.

## Capabilities

| Capability | Supported |
| --- | --- |
| Recommendation / Apply evidence | yes |
| Privacy-reduced activity summaries | yes |
| Context Health | yes |
| Optimized financial scan | yes |
| Glance data | yes |
| Invocation evidence | no |
| Plan context | yes |
| Transcript-reported rate limits | no |
| Statusline / Glance snapshot | yes |

## Privacy

- Parsing and aggregation run locally; raw prompts and responses are not returned by the parser registry.
- aibill never sits in the inference path and never stores, prints, or proxies provider credentials.

Fixtures and generated documentation must never contain a real transcript, prompt, response, credential, account identifier, session identifier, user name, or concrete developer-home path.

## Limitations

- Malformed lines are skipped and reported.
- Incomplete token shapes remain unpriced with missing financial evidence instead of becoming $0.

## Recorded fixture IDs

- `claude-code-v1`

The recorded fixtures are minimal synthetic reproductions of supported shapes. They are regression evidence for the reader—not provider billing evidence.
