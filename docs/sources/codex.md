<!-- GENERATED by scripts/generate-local-agent-source-docs.mjs. Do not edit by hand. -->

# Codex local transcripts

[All local agent sources](./README.md)

| Property | Registry value |
| --- | --- |
| Format ID | `codex` |
| Provider | `openai` |
| Format | `JSON Lines under ~/.codex/sessions/YYYY/MM/DD/rollout-*.jsonl` |
| Default local root | `~/.codex/sessions` |
| Discovered files | `rollout-*.jsonl` |
| Presence-only signal | `none` |
| Financial reader | `bounded_event_jsonl` |
| Reader validation | `live_verified` |
| Priced financial evidence | `estimated` |
| Unpriced financial evidence | `missing` |
| Normalized source | `Local agent session logs` |
| Observed from | `codex transcript JSONL (this machine)` |
| Usage granularity | `daily_aggregate` |

Local parsing was replayed against live logs; total-only token shapes and unknown aliases remain missing rather than becoming estimated $0.

Reader validation describes how the parser has been exercised; it never upgrades the evidence quality of a financial number. Source freshness and the latest sanitized reader error remain runtime observations shown by `aibill doctor --sources`, not static claims from this page.

## How aibill reads it

- Read the Codex event stream and retain the root session identity, model, and latest cumulative event_msg/token_count evidence.
- Never sum earlier running token counters; forked sessions subtract a supported inherited cumulative baseline.
- Use a bounded proof-based financial reader for init/cache while the full reader retains privacy-reduced activity and optional invocation evidence.

## Fields read

- session metadata, timestamps, model, and cumulative/last-turn token usage
- transcript-reported rate-limit windows when present
- tool-call metadata for local attribution and optional privacy-safe invocation counts

## Evidence boundary

### Verified about the reader

- The full and optimized financial readers have been replayed against live local Codex logs.
- Synthetic recorded fixtures lock cumulative, last-turn, and rate-limit normalization.

### Estimated financial evidence

- Supported token components are priced at published API rates as API-equivalent value.

### Not verified

- API-equivalent value is not billed spend, subscription cost, savings, or ROI.
- A transcript-reported limit is plan-capacity evidence, not a provider invoice.

## Capabilities

| Capability | Supported |
| --- | --- |
| Recommendation / Apply evidence | yes |
| Privacy-reduced activity summaries | yes |
| Context Health | yes |
| Optimized financial scan | yes |
| Glance data | yes |
| Invocation evidence | yes |
| Plan context | yes |
| Transcript-reported rate limits | yes |
| Statusline / Glance snapshot | yes |

## Privacy

- Parsing and aggregation run locally; raw prompts and responses are not returned by the parser registry.
- aibill never sits in the inference path and never stores, prints, or proxies provider credentials.

Fixtures and generated documentation must never contain a real transcript, prompt, response, credential, account identifier, session identifier, user name, or concrete developer-home path.

## Limitations

- Only rollout-*.jsonl files are parsed as Codex sessions.
- Incomplete, regressing, or total-only token shapes remain unpriced with missing financial evidence.

## Recorded fixture IDs

- `codex-v1`

The recorded fixtures are minimal synthetic reproductions of supported shapes. They are regression evidence for the reader—not provider billing evidence.
